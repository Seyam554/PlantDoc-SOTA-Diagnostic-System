# FPGA student pipeline - extra deps on top of the repo's torch/torchvision.
# NOTE: the checked-in .venv is broken (created on another machine, points at a
# missing Python). Recreate it:  py -3.11 -m venv .venv  &&  .venv\Scripts\activate
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r fpga/requirements-fpga.txt

# --- training / KD / eval ---
timm>=1.0.7                # stronger teacher (ConvNeXt) + backbones
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66
pillow>=10.0

# --- quantization ---
brevitas>=0.11.0          # INT8 QAT + PTQ (AdaRound/CLE/bias-corr) + QONNX export
qonnx>=0.4.0

# --- ONNX / hls4ml path ---
onnx>=1.16
onnxruntime>=1.18
# hls4ml>=1.0.0           # install only on the machine with Vitis HLS on PATH

# --- FINN ---
# not pip-installed here: use the FINN Docker image
#   git clone https://github.com/Xilinx/finn && cd finn && ./run-docker.sh
